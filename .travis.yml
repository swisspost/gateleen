language: java
jdk:
  - oraclejdk8
env:
  - GRADLE_OPTS="-Dorg.gradle.daemon=false"
services:
   - redis-server
install: echo "skip 'gradle assemble' step"
script: 
   - ./gradlew build --continue
   - ./gradlew :gateleen-test:test -PintegrationTests -q
   - ./gradlew jacocoTestReport
after_success:
  - bash <(curl -s https://codecov.io/bash)
